[database]
# Absolute SQLite path to use instead of the default ~/.local/share/todui/todui.db
# path = "/Users/you/.local/share/todui/todui.db"

[theme]
# modes: dark, light, beige
mode = "dark"
# accents: black, blue, cyan, gray, green, magenta, red, white, yellow
accent = "cyan"

[pomodoro]
focus_minutes = 25
short_break_minutes = 5
long_break_minutes = 15
notify_on_complete = true

[keys]
up = ["up", "k"]
down = ["down", "j"]
toggle_done = ["space", "x"]
history = ["H"]
pomodoro = ["p"]
